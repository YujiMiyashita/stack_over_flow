<%= form_for(@answer, url: question_answer_path) do |f| %>

  <%= f.label :content %>
  <%= f.text_area :content %>

  <%= image_tag @answer.image %>
  <%= f.file_field :image %>
  <%= f.hidden_field :image_cache %>

  <%= f.submit "回答を編集"%>
<% end %>
