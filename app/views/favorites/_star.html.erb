<%= form_for(current_user.favorites.build(question_id: question.id), method: :post, data: { remote: true }) do |f| %>
  <%= f.hidden_field :question_id %>
  <%= f.submit "★", class: "btn btn-block" %>
<% end %>
