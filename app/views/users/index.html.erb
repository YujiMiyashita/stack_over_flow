
<div class="container">
    <div class="col-sm-10 col-sm-offset-1 user_index">
      <h4>ユーザ</h4>
      <hr>

<% @users.each do |user| %>
  <p class="user">
  <%= link_to profile_img(user),user if profile_img(user) %>
  <%= link_to user.name,user %>
  </p>
<% end %>

</div>
</div>
